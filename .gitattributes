# 默认按 Git 自动判断
* text=auto

# 以下文件在 Linux 容器里执行 / 解析，必须强制 LF，
# 否则 Windows 开发者 checkout 后会被 core.autocrlf 转成 CRLF，
# 导致 shell 脚本 `/bin/sh\r` 找不到、Dockerfile 构建行为不一致等问题
*.sh          text eol=lf
Dockerfile*   text eol=lf
*.yml         text eol=lf
*.yaml        text eol=lf
.env*         text eol=lf
